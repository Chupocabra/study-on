{% extends 'base.html.twig' %}

{% block title %}Курсы/Редактировать/{{ course.name }}{% endblock %}

{% block body %}
    <h1>Редактирование курса {{ course.name }}</h1>

    {{ include('course/_form.html.twig', {'button_label': 'Редактировать'}) }}

    <a href="{{ path('app_course_index') }}">Вернуться к курсам</a>

    {{ include('course/_delete_form.html.twig') }}
{% endblock %}
