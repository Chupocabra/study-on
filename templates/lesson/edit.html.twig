{% extends 'base.html.twig' %}

{% block title %}Редактирование {{ lesson.name }}{% endblock %}

{% block body %}
    <h1>Редактировать {{ lesson.name }}</h1>

    {{ include('lesson/_form.html.twig', {'button_label': 'Редактировать'}) }}

    <a href="{{ path('app_course_show', {'id': lesson.course.id}) }}">Вернуться к курсу</a>

    {{ include('lesson/_delete_form.html.twig') }}
{% endblock %}
